# NEXT_SESSION_PROMPT.md

> This file is generated/overwritten during handoff. The initial version below is a safe generic bootstrap prompt.

You are taking over implementation of this repository from another AI coding agent.

Act as the implementation engineer; the human is the product/directing authority.

Before changing code:

1. Read `AGENTS.md` completely and follow it.
2. Read `PROJECT_STATE.md`.
3. Run/inspect `git status` and recent commit history.
4. Verify that `PROJECT_STATE.md` matches the actual repository.
5. Read only the relevant parts of `PROJECT.md`, `ROADMAP.md`, `ARCHITECTURE.md`, `docs/adr/`, `docs/specs/` and `.agents/skills/` needed for the current work.
6. Report your concise understanding of the current objective, repository state and exact next action.
7. Then continue from the **Exact next action** in `PROJECT_STATE.md` unless the director gives a newer instruction.

Do not assume the previous chat is available. Do not discard unexplained changes. Do not claim validation that you have not run. Keep the Markdown state files synchronized as durable project facts change.
