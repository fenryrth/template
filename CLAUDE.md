@AGENTS.md

# Claude Code adapter

The canonical project instructions are in `AGENTS.md`.

Project skills are stored in `.agents/skills/` for cross-agent portability. If they are not discovered as native Claude Code skills, inspect the skill descriptions there and read the relevant `SKILL.md` manually before performing a matching workflow.

Do not duplicate project rules into this file.
